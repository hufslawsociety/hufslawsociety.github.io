# 외대법학회 홈페이지 (HUFS Law Society)

한국외국어대학교 법학회 소개용 원페이지(one-page) 웹사이트를 만드는 프로젝트.

## 참고 파일

`~/Downloads/hufs-law-society.html` — 동일한 컨셉으로 먼저 만들어본 시안. **이 프로젝트의 결과물은 최소한 이 파일과 동등하거나 더 나은 완성도를 목표로 한다.** 새 파일을 만들거나 기존 코드를 고칠 때 막연한 판단이 서지 않으면 이 파일의 구조·톤·디테일을 기준으로 삼는다.

이 파일은 폰트/로고 이미지가 base64로 인라인되어 있어 3MB가 넘는다. 통째로 열지 말고 필요한 구간만 `sed -n`으로 잘라 읽을 것 (한 줄에 데이터가 몰려있는 지점 — 헤더 favicon, 브랜드 엠블럼 이미지, HERO_IMGS 배열 — 은 특히 피할 것).

## 시안이 보여주는 완성도 기준

- **디자인 시스템**: 남색(`--navy:#142c46`, `--navy-dark:#0b1929`, `--navy-light:#24466c`) + 골드(`--gold:#b9975b`, `--gold-light:#e0c894`) 조합. 배경은 아이보리에 가까운 밝은 회색(`--bg:#f6f6f4`), 카드는 흰색, 보더는 `#e6e4de`. 이 팔레트를 그대로 계승하거나, 다크모드 대응 시 이 톤을 유지하며 확장한다.
- **타이포그래피**: 본문/UI는 Pretendard Variable (jsdelivr CDN), 한글 강조 요소(비전 워드마크 등)는 Noto Sans KR. `word-break:keep-all`로 한글 줄바꿈 처리, `letter-spacing` 음수값으로 타이트한 트래킹.
- **섹션 구성**: header(고정 네비, 블러 배경) → hero(이미지 갤러리 + 워드마크 + 스탯) → about(연혁 요약, 비전) → advisor(지도교수 소개 카드) → history(타임라인, 남색 배경) → org(조직도, 임원진 카드 그리드) → activities(활동 카드 그리드 + 상세 모달, 이미지 크로스페이드) → notice(공지 리스트 + 모집 CTA) → contact(지도 iframe + 연락처) → footer.
- **인터랙션**: 순수 vanilla JS(프레임워크 없음). 히어로 이미지 갤러리, 활동 카드 이미지 자동 슬라이드(`IntersectionObserver`로 화면 밖일 때 정지), 활동 상세 모달(포커스 트랩 없이 backdrop + 썸네일 전환), 부드러운 스크롤.
- **반응형**: 브레이크포인트 1100 / 1000 / 920 / 560px에서 그리드 컬럼 축소, 네비 숨김, 폰트·패딩 축소. 항상 모바일에서 실제로 확인할 것.
- **다크모드**: 시안 파일 자체는 다크모드를 지원하지 않는다(`prefers-color-scheme` 없음). 이 프로젝트에서는 **다크모드를 반드시 새로 설계**해야 한다 — 남색/골드 아이덴티티가 다크 배경에서도 자연스럽게 이어지도록 톤을 조정하고, 라이트/다크 양쪽에서 대비(contrast)와 가독성을 검증한다.

## 기술 스택 / 작업 방식

- 특별한 지시가 없는 한 시안과 동일하게 **빌드 도구 없는 순수 HTML/CSS/JS**로 작업한다. 프레임워크(React 등) 도입은 사용자가 명시적으로 요청할 때만 고려한다.
- 이미지/로고는 시안처럼 base64 인라인도 가능하지만, 파일이 비대해지고 편집이 불편해지므로 프로젝트 규모가 커지면 `assets/` 디렉토리로 분리하는 것을 우선 검토한다.
- 웹폰트는 Pretendard(CDN) + Noto Sans KR(Google Fonts) 조합을 기본으로 하되, 실제 서비스 도메인에서 CDN 접근이 막힐 가능성을 고려해 필요시 자체 호스팅도 검토한다.

## 검증

- UI/반응형 변경 후에는 브라우저에서 실제로 열어 데스크톱과 모바일 뷰포트 양쪽, 라이트/다크 모드 양쪽을 확인한다.
